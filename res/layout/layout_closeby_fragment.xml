<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/screen_bg_color"
    android:orientation="vertical"
    android:paddingTop="8dp" 
    android:id="@+id/closebyfragment"
    >
  <CheckBox 
        android:id="@+id/layout_map_list_switch_activate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_gravity="center|right"
        android:button="@drawable/switch_shared_map_list_selector"
        android:layout_margin="2dp"
            />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:background="@android:color/white"
        android:orientation="vertical" >
        
        <ExpandableListView
            android:id="@+id/expandable_listview"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@drawable/four_border_bg"
            android:childDivider="@android:color/white"
            android:dividerHeight="0dp"
            android:scrollbarSize="2dp"
            android:fadeScrollbars="true"
            android:groupIndicator="@null"
            android:scrollbarThumbVertical="@drawable/ic_scroll_bae_image" >
        </ExpandableListView>
        <!-- <ListView
            android:id="@+id/google_listview"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@drawable/four_border_bg"
            android:childDivider="@android:color/white"
            android:dividerHeight="0dp"
            android:scrollbarSize="2dp"
            android:fadeScrollbars="true"
            android:scrollbarThumbVertical="@drawable/ic_scroll_bae_image" >
        </ListView> -->
    </LinearLayout>

</LinearLayout>